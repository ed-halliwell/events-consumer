version: '3.9'

networks:
  events:
    name: events

services:
  producer:
    container_name: events-consumer
    image: event-consumer
    build:
      context: ./
      dockerfile: ./Dockerfile
    networks:
      - events
    ports:
    - '4001:4001'
    environment:
      NODE_ENV: production
      PORT: '4001'
      QUEUE_URL: amqps://yttbbsxm:uWrk6gowG999I6WsxPzlZlnCy5pac8TR@kangaroo.rmq.cloudamqp.com/yttbbsxm
      QUEUE_NAME: eventdriven
